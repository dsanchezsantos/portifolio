import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  botao: {
    width: '70%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    paddingVertical: 8,
    borderRadius: 12,
    marginBottom: 24
  },
  botaoTexto: {
    color: 'black',
    fontSize: 20,
    fontWeight: 600
  }
});